import { Layout } from 'antd';

const { Footer } = Layout;

const LayoutHeader = ({}) => {
    return (
      <Footer style={{ textAlign: 'center' }}>Bara Recept ©2019 Created by Joel Gullander</Footer>
    )
}

export default LayoutHeader